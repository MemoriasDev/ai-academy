# Supabase Configuration (Client-safe)
# Get these values from your Supabase project dashboard
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# SECURITY NOTE:
# Variables prefixed with VITE_ are exposed to the client bundle
# The anon key is designed to be public and safe for client-side use
# It only works with Row Level Security (RLS) policies enabled
# 
# NEVER add service role keys with VITE_ prefix:
# VITE_SUPABASE_SERVICE_ROLE_KEY=... (NEVER DO THIS)
# SUPABASE_SERVICE_ROLE_KEY=... (only for server-side/tools, no VITE_ prefix)

# For local development only:
# Copy this file to .env.local and fill in your actual values
# .env.local is gitignored and won't be committed
